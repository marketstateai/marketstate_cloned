# Ops Infra

Infrastructure-as-code, environment bootstrap, and deployment manifests.

## Terraform Layout

- `terraform/gcp/currency-api-cloudrun`: currency API deployment (Artifact Registry + Cloud Run) managed with Terraform and helper scripts.
