# Legacy Location

This directory is no longer the canonical home for backend database and function assets.

Current locations:

- Portfolio bootstrap SQL: `services/core/portfolio/sql`
- Watchlist demo write policies: `services/core/watchlists/scripts`
- Provider-specific test scripts and env template: `ops/providers/supabase`
- Database migrations and seed assets: `ops/platform/database`
- Supabase CLI adapter project: `supabase/`
